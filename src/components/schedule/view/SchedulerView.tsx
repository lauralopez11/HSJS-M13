'use client';

import SchedulerViewFilteration from './SchedulerViewFilteration';

export default function SchedulerView() {
  return (
    <div className='flex flex-col gap-6'>
      <SchedulerViewFilteration />
    </div>
  );
}
